# PROJECT_SCOPE.md (한국어 번역본)

_(앱 이름: "Andy")_

> 이 파일은 `PROJECT_SCOPE.md` 원본(영어)의 한국어 번역본입니다. 원본이 수정되면 이 파일도 함께 업데이트해야 합니다.

## 미션 정렬 (Mission Alignment)

여기서 부/제품을 만드는 건 목적이 아니라 수단임. 이 앱 자체도 직접적인 임팩트 도구임: 같은 아키텍처가 네트워킹 회상, 클라이언트 회상, **그리고 고양이 위탁보호 기록**까지 다 커버함 — 즉 "내가 상호작용하는 누구/무엇에 대해서든 기억하기"로 일반화됨. 이 일반화를 데이터 모델에 유지할 것("contact"으로 하드코딩하지 말고, 비용이 크지 않은 선에서 내부적으로는 "entity"로 다룰 것).

## 성공 신호 (Success Signal)

_(이 스코프 문서에 빠져있던 유일한 PRD 요소 — 문제/유저는 이미 아래 One-liner에 있고, Non-goals는 이미 "Won't Have"에 있음)_

실사용 데이터가 나오기 전에 추측성 KPI 숫자로 채우지 말 것. 1주차의 신호는 지표가 아니라 질문임:

> 실제로 7일 써본 후: 이 앱을 **시키지 않아도** 알아서 찾게 되나, 아니면 나 자신한테 "써야 하는데" 하고 상기시켜야 하나?

전자라면 — 이걸 진짜 숫자(D7 리텐션, 주당 음성 노트 개수, "맞는 사람 찾음" recall-검색 성공률)로 바꿔서 V1.1부터 추적할 것. 후자라면 그것도 진짜 신호임: 핵심 루프가 아직 끈끈하지 않다는 뜻이고, 고칠 건 UX/마찰이지 기능 추가가 아님.

## 한 줄 정의 (One-liner)

사람(또는 위탁보호 중인 고양이)에 대해 음성으로 메모 → LLM이 검색 가능한 프로필로 구조화 → 자연어 검색, Siri 단축어, 홈 화면 위젯으로 이름이 기억 안 나도 나중에 회상.

## 제품 내러티브 — "The Briefing (브리핑)"

Andy가 Miranda의 귀에 대사(ambassador)의 이름과 핵심 정보를 미리 속삭여주는 것 — 그가 인사하기 직전에. 이 앱이 할 일은 어떤 사람에 대해 정확히 알아야 할 것을, 정확히 필요한 시점에 보여주는 것임 — 만나기 전(캘린더 기반 브리핑)과 만난 직후(새로 기억할 것을 남기라는 넛지) — 유저가 직접 찾아다닐 필요 없이.

**타임라인: 10~12일로 연장** (원래 7일에서) — 캘린더 기반 브리핑/넛지와 크로스 프로필 멘션 검색을 제대로 넣기 위함. 둘 다 이제 stretch goal이 아니라 Must-have임.

## Decision Framework 요약

```
Goal       : 솔로로, 약 1주일 안에 앱스토어에 완성도 있는 실제 제품을 낸다.
Constraints: 솔로 개발자, 1주일, Apple Individual 계정 이미 승인됨,
             Claude Code를 주 개발 도구로 사용.
Leverage   : 음성에서 파생된 노트에 대한 시맨틱(벡터) 검색 = 핵심 해자(moat).
             TypeScript로 통일된 스택(Expo + Convex) = AI 코딩 에이전트가
             실수할 수 있는 표면적이 가장 작음.
Risk       : iOS는 실제 셀룰러 통화 중에 발신자 프로필을 자동으로 띄울 수
             없음(불가능함을 확인 — 아래 참고). 이 스코프는 이걸 Siri
             단축어 + 위젯으로 대체하는데, 이건 이번 주 안에 실제로 달성
             가능함.
```

## 이 스코프에 반영된 Reality Check

1. **iOS 전화 중 자동 팝업은 없음.** Call Directory Extension은 번호가
   _시스템 연락처에 이미 없을 때만_ 작동하고, 커스텀 CallKit UI는
   VoIP 전용임 — 둘 다 이미 알고 있는 연락처의 실제 셀룰러 통화에는
   적용 안 됨. 이쪽으로는 개발하지 말 것; 시간을 아무리 써도 안 됨.
2. **Android 전화 오버레이는 V1.1로 연기**, 현재 Play 콘솔 정책
   (`READ_CALL_LOG`/`READ_PHONE_STATE`, Core App 선언 리스크)을
   실제로 다시 확인한 후에. 정책 재확인 없이 주중에 시작하지 말 것.
3. **Apple 심사 시간은 우리가 통제할 수 없음.** "코드 완성" 이후의
   심사 대기 시간을 예산에 반영할 것 — 코드 완성 ≠ 제출 ≠ 출시.
4. **SMS/문자 읽기는 iOS에서 불가능함, 전화 팝업과 같은 범주.**
   서드파티 앱이 Messages 콘텐츠를 읽는 공개 API가 없음. 이쪽으로
   개발하지 말 것. Android의 SMS 권한군은 Play에서 가장 강하게
   제한되는 그룹임 — 리스크 감수할 가치 없음.
5. **Gmail 인박스 자동 읽기는 Google OAuth "sensitive scope" 검증
   (CASA 보안 심사)이 필요함** — 이건 수일~수주 걸릴 수 있고 우리
   통제 밖이라, 예측 불가능한 앱스토어 심사와 같은 실패 유형임.
   이번 제출을 절대 막지 않도록 V1.1+로 명확히 연기함. 후속
   **이메일 초안 작성**(이미 저장된 노트 기반, `mailto:` 딥링크로
   전달)은 **OAuth가 전혀 필요 없어서** V1에 포함됨.
6. **캘린더(EventKit)가 사실상 "전화" 비전에 대한 진짜 답임.**
   전화번호와 달리 캘린더 이벤트엔 이미 그 사람 이름이 붙어있음 —
   신원 추측 문제도, CallKit 제약도, 특수 entitlement도 없음.
   이번 개정에서 가장 레버리지가 높은 추가사항.
7. **iOS는 앱당 로컬 알림을 최대 64개까지만 대기시킬 수 있음.**
   모든 캘린더 이벤트에 대해 무제한으로 브리핑/넛지 쌍을 예약하지
   말 것 — 앞으로 다가올 매칭된 미팅 약 20~25개에 대해서만 예약하고
   앱이 포그라운드로 올 때/캘린더 동기화 시마다 갱신할 것.
8. **Google Calendar용 별도 연동은 필요 없음.** EventKit은 iOS
   캘린더 앱에 동기화된 모든 캘린더를 읽어옴 — 유저가
   설정 → 캘린더 → 계정에서 Google 계정을 추가해뒀다면 Google
   Calendar도 포함됨. 권한 요청 한 번, API 하나로 Apple + Google +
   대부분의 CalDAV 캘린더를 커버함. 별도의 OAuth 기반 Google
   Calendar 연동을 만들지 말 것 — 중복임.
9. **커스텀 Siri 웨이크워드("Hey Andy")는 불가능함.** iOS는
   서드파티 앱이 자기만의 상시 대기(always-listening) 웨이크
   프레이즈를 등록하는 API를 제공하지 않음 — "Hey Siri"/"Siri"만
   유일한 시스템 웨이크워드임, 설계상 그러함. 이미 스코프에 있는
   Siri 단축어("Hey Siri, Andy에서 [이름] 찾아줘")가 App Intents를
   통한 올바른 패턴임 — 앱 이름이 발화되는 문구의 일부로 들어감.
   진짜 커스텀 웨이크워드는 쫓지 말 것.
10. **진짜 종단간 암호화(E2E)는 이 아키텍처와 호환되지 않음, 의도적인
    것이지 실수가 아님.** 추출 작업은 Claude(서버사이드, Convex
    `action` 안에서)가 노트 콘텐츠를 평문으로 읽어야만 가능함;
    Day One 스타일처럼 서버가 정말로 복호화를 못 하는 방식이면
    핵심 추출 파이프라인이 깨짐 — 추출을 온디바이스 모델로 옮기지
    않는 한(스코프 밖). V1은 표준 암호화(전송중 + 저장중)를 쓰고,
    zero-knowledge E2E는 쓰지 않음. 누가 물어보면 이 구분을 설명할
    준비를 해둘 것 — 이건 LLM 추출 기능을 위한 의도적인 트레이드오프지
    보안을 대충 한 게 아님.

### 선례 — 이 트레이드오프는 우리만의 편법이 아니라 업계 표준임

추측이 아니라 직접 조사한 내용:

- **Day One 자신**(우리의 벤치마크, 아래 참고)도 자사 AI 기능 문서에서, 종단간 암호화된 저널의 콘텐츠가 AI로 처리되는 동안엔 일시적으로 종단간 암호화되지 않는다고 명시함 — 기기에서 복호화된 다음 HTTPS로 AI 서비스에 전송되고, 처리된 후 Day One과 AI 제공업체 양쪽 서버에서 모두 삭제됨. 우리가 하는 것과 근본적으로 같은 트레이드오프 — 다만 우리는 추출 결과를 영구 저장한다는 점만 다름, 왜냐하면 구조화된 회상 자체가 우리 제품의 핵심이기 때문. (dayoneapp.com/guides/ai-features)
- **WhatsApp/Meta AI**도 정확히 같은 경계선을 그음: 사람과 사람 사이 메시지는 종단간 암호화가 유지되지만, Meta AI**한테** 보낸 메시지(또는 채팅에서 태그한 경우)는 명시적으로 그 보장 대상이 아님 — Meta가 유저가 어시스턴트에게 보낸 내용을 읽을 수 있음. 같은 패턴: 저장/전송은 E2E, AI가 콘텐츠를 읽어야 하는 순간엔 명시적 예외.
- **주류 헬스 인접 AI 앱들**(사진 기반 음식 기록, AI 웰니스 코치 등)도 같은 구조적 이유로 대체로 표준 암호화(전송중/저장중) + 접근 제어를 쓰지, zero-knowledge E2E를 쓰지 않음.
- **규제 관련 참고사항 (법률 자문 아님)**: 미국에서 대부분의 소비자용 헬스 인접 앱은 HIPAA 적용 대상 밖임(HIPAA는 의료기관/보험사/클리어링하우스와 그 협력업체를 커버하지, 일반 소비자 앱을 커버하지 않음), 다만 워싱턴주의 My Health My Data Act 같은 주(state) 소비자 건강 데이터 법의 적용 범위는 점점 넓어지고 있음. 이 앱이 반려동물 건강 지표와, 다른 사람의 건강/개인적 상황을 언급할 수도 있는 노트를 저장한다는 걸 감안하면, 넓게 공개 출시하기 전에 실제로 법률 자문을 받아볼 것 — 이 메모는 정보 제공용이지 컴플라이언스 판단이 아님.

## V1 기능 스코프 (MoSCoW)

### Must Have (반드시 있어야 함)

- [ ] **Clerk**을 통한 인증 — V1은 Apple Sign-In만 (README의 Tech Stack Decisions 참고)
- [ ] 음성 녹음 → 온디바이스/API 전사 → Claude 추출로 구조화된 프로필 생성 (이름, 관계 맥락, 핵심 정보, 태그, 처음 만난 날짜)
- [ ] 수동 프로필 생성/편집 (음성을 안 쓸 때의 대체 수단)
- [ ] **명함 사진 → 프로필** — 음성과 동일한 Claude 추출 액션에 이미지 입력만 다르게 줌; 사진에서 바로 이름/직함/회사를 추출 (거의 추가 비용 없음, 기존 파이프라인 재사용)
- [ ] **크로스 프로필 멘션 그래프 검색** — 시맨틱 검색이 프로필 대표 필드뿐 아니라 모든 *노트*에 걸쳐 실행됨. 그래서 "X 생일파티에서 만난 메타 개발자"가, 그 사람이 자기 프로필 없이 다른 사람 프로필 안에 언급만 돼 있어도 찾아짐. 아래 스키마 참고.
- [ ] 프로필별 타임라인 — 노트/상호작용의 시간순 목록
- [ ] iOS 연락처 동기화 — 프로필별 opt-in, 기존 연락처 읽기 + 선택적으로 노트/태그 다시 쓰기 (expo-contacts)
- [ ] **캘린더 브리핑** — 캘린더 읽기(EventKit — API 하나로 Apple + Google + CalDAV 커버, Reality Checks 참고), 참석자/제목 이름을 프로필과 퍼지 매칭, 미팅 전 로컬 알림(최근 노트 요약)과 미팅 후 넛지("오늘 A랑 어땠어? 새로 기억할 것?") 예약
- [ ] **팔로업 이메일 초안** — 저장된 노트로 초안 생성, `mailto:` 딥링크로 전달 (인박스 읽기 없음, OAuth 없음)
- [ ] 프로필별 수동 사진 첨부 (expo-image-picker)
- [ ] **반려동물/동물 지표 기록** — 프로필별 구조화된 시계열 하위 레코드(날짜, 지표 유형, 값, 단위), 자유 텍스트 노트와는 별개, 체중/건강 추적용
- [ ] **비밀번호/생체인증 앱 잠금** (expo-local-authentication) — 이 앱은 다른 사람의 동의 없이 그들에 대한 노트를 저장하니까, 잠금 화면은 신뢰의 기본 조건이고 추가 비용도 저렴함
- [ ] Siri 단축어 — "Hey Siri, Andy에서 [이름] 찾아줘" → 최근 노트 요약을 음성/텍스트로 반환
- [ ] 홈 화면 위젯 — 고정/최근 5개 프로필을 한눈에, **거기에 원탭 녹음 버튼이 있는 두 번째 위젯 변형 추가** (탭 → 앱이 곧바로 준비된 캡처 화면으로 열리고 즉시 녹음 시작; 앱을 아예 안 여는 진짜 백그라운드 녹음은 위젯 익스텐션으로는 신뢰성 있게 안 됨, 아래 Entry-Input Channels 참고)
- [ ] 유저 본인 기기들 간 실시간 동기화
- [ ] **아이패드 지원, 공짜** — `app.json`에서 디바이스 패밀리를 제한하지 않음; iPhone UI가 아이패드에서 스케일/호환 모드로 자동으로 돌아감. V1엔 전용 아이패드 레이아웃은 없지만 기능은 다 됨.

### Should Have (있으면 중요함)

- [ ] 간단한 RAG 챗봇 — "브라이언이랑 마지막에 무슨 얘기했지?" 같은 자유 형식 Q&A, 유저 본인의 노트에 근거함
- [ ] 태그/카테고리 시스템 (client / friend / networking / foster-animal / other), 태그와 날짜 범위로 검색 필터링 가능
- [ ] 동물 프로필에 흔한 지표 유형용 원탭 입력 버튼 (예: "체중", "병원 방문") — `metrics` 테이블 위에 얹는 템플릿형 단축 기능, 별개 기능 아님
- [ ] 프로필별 vCard 내보내기

### Could Have (V1.1 이후)

- [ ] Google Sign-In (실제로 의미 있어지는 Android 빌드와 함께 — README의 Tech Stack Decisions 참고)
- [ ] Gmail 인박스 자동 읽기 (Google OAuth sensitive-scope 검증에 막혀 있음 — 이걸 추진할 거면 이 프로세스는 그 자체로 수주가 걸리니 일찍 시작할 것)
- [ ] **이메일로 노트 생성(인바운드)** (Day One처럼, 전용 주소로 메시지를 BCC/전달) — 인바운드 이메일 인프라(도메인 + 파싱 웹훅) 필요, 실제로 되지만 만만치 않은 셋업
- [ ] **전용 Twilio 번호를 통한 문자로 노트 생성(인바운드)** (유저 본인의 Messages를 읽는 것과는 다름 — 그건 여전히 불가능함) — 기술적으로 접근 가능하나 비용/시간 문제로 연기
- [ ] **iOS Share Extension** — Photos, Safari, Files 등의 시스템 공유 시트에 나타남("Andy로 공유하기"). 공유된 사진은 Day 2에 만드는 명함용 Claude vision 추출을 그대로 재사용; 공유된 URL은 그냥 노트에 첨부만 함(자동 웹페이지 스크래핑/추출은 만들지 말 것 — 그건 별개의 더 큰 기능임). V1.1에서 진짜로 최우선 순위로 둘 가치 있음: 위젯의 App Group + 익스텐션 타겟 패턴을 재사용하고, Day 2 추출 파이프라인도 재사용함 — 워치 앱이나 네이티브 맥 앱보다 저렴하지만, 그래도 새로운 네이티브 타겟이라 다른 걸 빼지 않고 지금 10~12일 안에 공짜로 끼워넣을 정도는 아님.
- [ ] 자동 사진-프로필 얼굴 매칭 (온디바이스 ML, 별도 프라이버시 검토 필요)
- [ ] Android 전화 중 오버레이 (Play 정책 재확인 후)
- [ ] 멀티유저/공유 프로필 (예: 파트너와 공유하는 위탁보호 고양이 기록)
- [ ] 지도 뷰/캘린더 뷰, 스트릭 스타일 리마인더 — 이 앱 특성상 우선순위 낮음: 여기서 회상은 사람 중심이지 장소/날짜 중심이 아니고, 캘린더 기반 넛지가 이미 "뭔가 기록하라고 상기시키기"라는 역할을 일반적인 스트릭 리마인더보다 더 잘 해냄
- [ ] **진짜 인터랙티브 위젯 녹음** (앱을 아예 안 열고 녹음을 시작하고 계속 유지) — 원리상으론 가능하지만 위젯 익스텐션 프로세스가 오래 지속되는 라이브 마이크 세션을 안정적으로 유지하도록 만들어져 있지 않음; Must Have에 있는 딥링크 버전이 실제로 너무 느리게 느껴질 때만 재검토
- [ ] **애플워치 컴패니언 앱** — 확인 결과 진짜로 별도의 네이티브 Swift/SwiftUI 코드베이스가 필요함(React Native는 watchOS에서 아예 안 돌아감), 게다가 watchOS 자체의 인터랙티브 위젯/컴플리케이션 API는 Apple 개발자 포럼에 신뢰성 이슈가 기록돼 있음. 이건 폰 앱의 확장이 아니라 진짜 네 번째 플랫폼임. 폰에서의 핵심 경험에 대한 Success Signal부터 검증할 것 — 폰에서도 아직 끈끈하지 않은 루프를 위해 워치 앱을 만들지 말 것.
- [ ] **가벼운 웹 페이지를 통한 노트북 캡처** (네이티브 맥 앱이 아니라) — 같은 Convex 함수를 호출하는 최소한의 브라우저 페이지(노트 타이핑, 또는 Web Audio API로 녹음)면 "내 노트북에서 캡처하기" 니즈의 대부분을, Mac Catalyst나 React Native macOS(둘 다 진짜 별도 플랫폼 작업임)보다 훨씬 적은 비용으로 커버함

### Won't Have (이번 버전엔 없음)

- iOS 전화 중 자동 팝업 (불가능함, Reality Checks 참고)
- 유저 본인의 Messages를 읽는 SMS/문자 읽기 (iOS에서 불가능, Android에서도 정책 리스크 높음)
- 진짜 종단간(zero-knowledge) 암호화 — 서버사이드 LLM 추출과 호환 불가, Reality Checks 참고
- 영상 노트
- 한국어/영어 외 언어 전사

## User Flow (가볍게 — Day 1의 모호함을 없앨 정도만, 완전한 IA 문서는 아님)

**캡처 플로우**

```
녹음 탭 → 말하기 → 추출된 초안 확인(이름, 태그, 핵심 정보) →
확인/편집 → 저장
   (이름이 기존 프로필과 매칭되면 → 노트 추가; 새 이름이면 → 프로필 생성;
    Claude가 낮은 확신도로 매칭을 표시하면 → 유저에게 확인/병합 여부 질문)
```

**회상(Recall) 플로우**

```
검색창 → 자연어 쿼리 → 순위 매겨진 결과
   (본인 프로필 매칭과 "X의 프로필에서 언급됨" 매칭을 다르게 표시)
   → 프로필 진입 → 타임라인 뷰
```

**브리핑 플로우**

```
캘린더 이벤트가 프로필과 매칭됨 → 미팅 전 로컬 알림
(최근 노트 요약) → 미팅 시각 경과 → 미팅 후 넛지 →
넛지 탭 → 해당 프로필로 범위가 좁혀진 캡처 플로우가 열림
```

**화면 구성 (V1 최소 구성, Expo Router)**

```
/ (홈)                 → 고정/최근 프로필, 검색창, 녹음 버튼
/profile/[id]          → 타임라인, 태그, 지표(동물인 경우), 사진, 팔로업 이메일 버튼
/profile/[id]/capture  → 음성/수동 캡처, 이 프로필로 범위가 좁혀짐
/search                → 회상 검색 결과
/settings              → 연락처 동기화 토글, 캘린더 권한, 계정
```

## Entry-Input Channels (입력 채널)

아래 각 채널은 전부 _같은_ 캡처 → 추출 → 저장 파이프라인으로 들어오는 서로 다른 입구일 뿐임 — Claude 추출 액션은 입력이 어느 문으로 들어왔는지 모르고 신경 쓰지도 않음. 텍스트 기반 채널(SMS, 이메일, 웹 페이지)은 전사 단계를 건너뛰고 바로 추출로 감.

| 채널                      | 상태              | 비고                                                                                 |
| ------------------------- | ----------------- | ------------------------------------------------------------------------------------ |
| 인앱 녹음 버튼            | V1                | 핵심 플로우                                                                          |
| 홈 위젯 — 한눈에 보기     | V1                | 고정/최근 프로필                                                                     |
| 홈 위젯 — 원탭 녹음       | V1                | 탭 → 앱이 준비된 캡처 화면으로 열림 (앱을 아예 안 여는 방식은 아님, Could Have 참고) |
| 명함 사진                 | V1                | 동일한 추출 액션, 이미지 입력                                                        |
| 아이패드                  | V1, 공짜          | 호환 모드, 추가 작업 없음                                                            |
| Siri 단축어               | V1                | "Hey Siri, Andy에서 [이름] 찾아줘"                                                   |
| 전용 번호로 문자          | V1.1              | Twilio 번호 + 웹훅 필요                                                              |
| 전용 주소로 이메일        | V1.1              | 인바운드 파싱 웹훅 필요                                                              |
| 노트북 웹 캡처 페이지     | V1.1              | 네이티브 맥 앱보다 저렴                                                              |
| 애플워치 앱               | V1.1 이후         | 별도 네이티브 Swift/SwiftUI 코드베이스, 진짜 규모의 작업 — Could Have 참고           |
| 진짜 인터랙티브 위젯 녹음 | V1.1 이후 stretch | 딥링크 버전이 너무 느릴 때만                                                         |

## DB 스키마 (Convex) — 초안(DRAFT), Day 1 plan mode에서 검증할 것, 최종본 아님

```typescript
// convex/schema.ts
import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

export default defineSchema({
  profiles: defineTable({
    userId: v.id("users"),
    name: v.string(),
    entityType: v.union(v.literal("person"), v.literal("animal")),
    relationshipContext: v.optional(v.string()), // "client" | "friend" | "networking" | "foster" | ...
    tags: v.array(v.string()),
    firstMetDate: v.optional(v.string()),
    contactId: v.optional(v.string()), // 연결된 iOS 연락처 식별자
    photoStorageId: v.optional(v.id("_storage")),
    isStub: v.boolean(), // true = 언급만으로 자동 생성됨, 아직 직접 작성된 노트는 없음
  })
    .index("by_user", ["userId"])
    .searchIndex("search_name", { searchField: "name" }),

  notes: defineTable({
    userId: v.id("users"),
    profileId: v.id("profiles"), // 대표(primary) 프로필
    mentionedEntityIds: v.array(v.id("profiles")), // 부차적으로 언급된 대상들
    text: v.string(),
    embedding: v.array(v.float64()),
    source: v.union(
      v.literal("voice"),
      v.literal("manual"),
      v.literal("calendar_nudge"),
    ),
    createdAt: v.number(),
  })
    .index("by_profile", ["profileId"])
    .vectorIndex("by_embedding", {
      vectorField: "embedding",
      dimensions: 1536,
      filterFields: ["userId"],
    }),

  metrics: defineTable({
    userId: v.id("users"),
    profileId: v.id("profiles"),
    date: v.string(),
    metricType: v.string(), // "weight" | "vet_visit" | ...
    value: v.number(),
    unit: v.string(),
  }).index("by_profile", ["profileId"]),

  calendarLinks: defineTable({
    userId: v.id("users"),
    profileId: v.id("profiles"),
    calendarEventId: v.string(), // EventKit 이벤트 식별자
    meetingStart: v.number(),
    meetingEnd: v.number(),
    briefingNotificationId: v.optional(v.string()),
    nudgeNotificationId: v.optional(v.string()),
  })
    .index("by_profile", ["profileId"])
    .index("by_event", ["calendarEventId"]),
});
```

이건 Day 1을 위해 제안된 목표치임 — 처음부터 새로 설계하지 말고 이걸 구현하거나 여기서 벗어나는 부분을 플래그로 표시할 것, 그리고 Claude Code가 실제 쿼리 패턴을 본 뒤에도 이게 여전히 말이 되는지 확인할 것.

## Architecture (아키텍처)

```
음성/사진 입력 (녹음은 expo-audio, 명함 사진은 expo-image-picker)
   → 전사 (온디바이스 expo-speech-recognition, 또는 호스팅된 Whisper API)
   → Claude 추출 (Convex action, 서버사이드 키) — 음성 전사본 또는
     명함 이미지를 입력으로 받음
       → 대표 프로필 + 부차적 언급 대상들을 식별
   → Convex에 구조화된 형태로 기록 (profile + note + embedding + mentionedEntityIds)
   → Convex 벡터 인덱스 (프로필 단위가 아니라 노트 단위)
   → 회상: 자연어 쿼리 → 임베딩 → 벡터 검색 → 결과
       (프로필별로 그룹화, 멘션 히트는 별도로 표시)
   → 표출 지점: 인앱 검색 | Siri 단축어 | 홈 위젯 | RAG 챗봇

캘린더 브리핑 (병렬 경로)
   EventKit 캘린더 읽기 (API 하나로 Apple + Google + CalDAV 커버) →
   참석자/제목을 프로필과 퍼지 매칭
   → 매칭된 다가오는 미팅마다 로컬 알림 2개 예약:
       (a) 미팅 전: 최근 노트 요약
       (b) 미팅 후: "새로 기억할 것 있어?" 넛지
   → 넛지 응답 → 음성/텍스트 캡처 → 위와 동일한 추출 파이프라인

팔로업 이메일 (요청 시)
   프로필의 저장된 노트 → Claude가 팔로업 초안 작성 → mailto: 딥링크
   (인박스 접근 없음, OAuth 없음)
```

## 기술 스택 (Tech Stack)

- **프론트엔드**: Expo (React Native) + TypeScript
- **백엔드**: Convex (DB, 함수, 실시간 동기화, 벡터 검색, 파일 스토리지)
- **인증**: Clerk — V1은 Apple Sign-In만 (왜 이렇게 결정했는지, 왜 Convex Auth/Firebase/Supabase가 아닌지는 README의 Tech Stack Decisions 참고)
- **LLM**: Claude API (추출/비용 절감용 Haiku, 챗봇 품질용 Sonnet; 명함 사진 추출용 멀티모달) — Convex action에서만 호출, 키는 절대 온디바이스에 없음
- **연락처**: expo-contacts
- **음성**: 녹음은 expo-audio (expo-av는 Expo SDK 55부터 제거됨 — 쓰지 말 것), 온디바이스 전사는 expo-speech-recognition (또는 호스팅된 Whisper API), expo-speech는 TTS 전용 (Siri 단축어의 음성 응답용, 전사용 아님)
- **보안**: expo-local-authentication (비밀번호/생체인증 앱 잠금)
- **단축어/위젯**: 네이티브 iOS App Intents (작은 native/Expo config plugin 필요 — 여기에 실제 시간을 넉넉히 배정할 것, 스택 중에서 "그냥 되는" 정도가 제일 낮은 부분임)

## 10~12일 계획

_(길이는 그대로 — 아래의 Day One에서 영감받은 추가사항들은 기존 일정에 끼워넣을 만큼 저렴함: 명함 추출은 Day 2 파이프라인을 재사용하고, 비밀번호 잠금은 Day 9에 몇 시간 추가되는 정도)_

| Day   | 초점                                                                                                                           |
| ----- | ------------------------------------------------------------------------------------------------------------------------------ |
| 1     | Expo + Convex 프로젝트 초기화, Clerk 인증(Apple Sign-In), 스키마(`mentionedEntityIds`, `Metric` 포함), CI 스켈레톤             |
| 2     | 음성 캡처 → 전사 → Claude 추출 파이프라인 (대표 프로필 + 부차적 언급) + **동일한 추출 액션의 두 번째 입력으로 명함 사진 추가** |
| 3     | 프로필 CRUD + 타임라인 UI + 수동 입력 대체 수단 + 반려동물 지표 기록 UI + 원탭 입력 버튼                                       |
| 4     | 노트 단위 벡터 인덱스 + 크로스 프로필 멘션 검색 + RAG 챗봇                                                                     |
| 5     | 연락처 동기화 + 수동 사진 첨부 + 팔로업 이메일 초안(mailto)                                                                    |
| 6     | 캘린더 읽기(EventKit) + 참석자/프로필 퍼지 매칭                                                                                |
| 7     | 로컬 알림 예약(미팅 전 브리핑, 미팅 후 넛지) + 넛지에서 시작하는 캡처 플로우                                                   |
| 8     | Siri 단축어 + 홈 위젯 (한눈에 보기 변형 + 원탭 녹음 변형, 같은 위젯 익스텐션 작업)                                             |
| 9     | 태그 + 검색 필터, vCard 내보내기, 알림 개수 가드레일(64개 상한), **비밀번호/생체인증 잠금**, 버그 바쉬                         |
| 10    | 프라이버시 문자열, `app-store-reviewer` 검토, 다듬기                                                                           |
| 11~12 | 버퍼 — 실제 기기로 엔드투엔드 테스트, EAS 빌드, TestFlight, 제출 (버퍼는 밀린 일정을 흡수하는 용도, 건너뛰지 말 것)            |

## 다시 짚어볼 남은 리스크

- 한국어 음성 전사 정확도 (Day 9가 아니라 Day 2에 일찍 테스트할 것)
- 규모가 커졌을 때 건당 Claude API 추출 비용 (MVP엔 문제없음, 스케일 전에 모델링할 것)
- 연락처 + 마이크 + **캘린더 + 사진** 접근에 대한 앱스토어 프라이버시 심사 — 정확하고 솔직한 사용 목적 문자열을 작성할 것 (`app-store-reviewer` 서브에이전트 참고)
- 캘린더 참석자/제목 이름 매칭 품질 — 가짜 데이터가 아니라 실제 지저분한 본인 캘린더로 일찍(Day 6) 테스트할 것
- 멘션 그래프 추출 품질 — Claude가 "이 노트는 A라는 사람에 대한 것"과 "이 노트에서 B라는 사람이 지나가듯 언급됨"을 안정적으로 구분해야 함; 깔끔한 타이핑 텍스트가 아니라 실제 음성 전사본으로 신뢰하기 전에 검증할 것
