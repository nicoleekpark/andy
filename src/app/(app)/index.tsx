import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="Andy"
      note="Tap record to remember someone new, or search for someone you already have."
    />
  );
}
